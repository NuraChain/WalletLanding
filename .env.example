# Canonical origin for this build. Drives <link rel="canonical">, Open Graph
# URLs, JSON-LD, robots.txt and sitemap.xml — change it in exactly this one
# place. No trailing slash.
# TODO(deploy): replace with the real production domain.
VITE_SITE_URL=https://nurawallet.app
